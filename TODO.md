# TODO

> Let's see how far we can climb up the k-WL ladder. Use Cai et al. and the BREC benchmark to construct a dataset of graphs of increasing k-WL indistinguishability (say 5 graphs for each k). Then benchmark the following models: 1. passive photonic QMapNN with a single photon per cell, 2. passive photonic QMapNN with a dual-rail Bell state input at each time step, 3. active photonic QMapNN with both classical and quantum messages, 4. Qubit QMapNN with a qubit circuit and quantum messages (only the prediction measured), 5. Qubit QMapNN with both classical and quantum messages. In each case, the model should be simply a functor turning the CMap representing the graph into an optyx.interaction.CMap. The interpretation of the cells should be physical, i.e. a CPTP map + optionally measurements and classical feedforward. You can use the modal GPUs for large graphs and possibly some tensor network approximation methods if the graph is too big.

- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 Dataset: pairs of increasing k-WL indistinguishability from Cai-Fürer-Immerman and BREC, about five per rung, the claimed WL level verified by our own 1-WL / 2-FWL / 3-FWL implementations where feasible
- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 The graph as a CMap and the five models as functors into `optyx.interaction.CMap`, every cell a physical CPTP map (+ measurement and feed-forward where the model has them): passive single-photon, passive dual-rail Bell drive, active photonic with classical + quantum messages, qubit unitary with measured prediction, qubit with classical + quantum messages
- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 Exact few-photon certificates for the photonic models (ported from beyond_3wl) validated against optyx's own contraction of the functor image on small graphs
- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 Benchmark all five models over the ladder, tensor-network contraction (quimb, GPU via Modal if available) for the qubit models, trimming expensive cells with an explicit log rather than silently
- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 Committed results (CSV + report) with validation: probability conservation and relabelling invariance, exact zeros stated as measured
- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 06:45 Lint and tests green (fast smoke test in `test/`), draft PR stacked on #61

> Once you see that the model doesn't separate, don't keep runnning it for higher ks. Focus on the ones that climb

- [WIP] @01AdC8wUqUgpSYLjK56Dvhy9-2026-08-22 14:45 Prune the benchmark accordingly: passive and Bell stop at the 2-FWL rung, active one-hop stops at the CFI pairs it measures zero on; add the relayed-broadcast (flooding) active cell — the one that can still climb — and run it up the CFI rungs pair by pair, stopping at the first rung it fails
