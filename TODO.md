# TODO

> Let's write a new PR in optyx with a notebook describing all the features that are coming up with the other PRs. The idea is to write  an introduction to photonics hardware and how to simulate it in optyx, it will be the first notebook in the documentation, or coul even be used in the readme. Title: "What is a channel?" Structure: 1. gates (BS, Phase, Interferometer, from_unitary) 2. states (Fock states, dual rail states), 3. effects (Observables, from_bosonic_operator), 4. noise (Photon loss, noise corresponds to discarding an environment), 5. distinguishability (dilate, photons are distinguished via their internal state), 6. Measurement (classical wires, simulating the current setups state>> unitary>> measure), 7. Classical control (controlled gates and feedforward, a simple example with two gates, one measures one is controlled), 8. Feedback (Add classical or quantum feedback loops, give the initial state, unroll and take the fixpoint) 9. The universal setup. At the end we reach the full picture: a setup with a state psi, plugged into a classically controlled isometry U from M + L to M + L + E with a classical control of dimension , where E is a discarded environment (modelling the loss), and L is the number of qmode feedback loops,  the M output qmodes are measured via Measure(qmode) and the classical modes are applied a classical feedforward function into X + C, where X is the number of classical outputs and the outputs in C are plugged back as control for the unitary U. bThe notebook ends by defining this as a class and then drawing and simulating an instance.

- [WIP] @lscc1x-2026-08-05 Write `docs/notebooks/what-is-a-channel.ipynb` following the nine-section structure above, executed end to end on top of the feedback/fixpoint branch (PRs #12, #15, #26)
- [ ] Make it the first notebook in `docs/notebooks.rst`
